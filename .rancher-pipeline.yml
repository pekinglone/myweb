stages:
- name: Build
  steps:
  - publishImageConfig:
      dockerfilePath: ./dockerfile/Dockerfile
      buildContext: .
      tag: myweb-1:${CICD_EXECUTION_SEQUENCE}
      registry: 127.0.0.1:34422
    env:
      PLUGIN_DEBUG: "true"
- name: Deploy
  steps:
  - applyYamlConfig:
      path: ./yaml/k8s-myweb.yaml
timeout: 60
